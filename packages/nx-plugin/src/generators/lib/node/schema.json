{
  "$schema": "http://json-schema.org/schema",
  "$id": "NodeLib",
  "cli": "nx",
  "title": "Create a Node library",
  "description": "Create a Node library",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Library name.",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like to use for the library?",
      "pattern": "^[a-zA-Z].*$"
    },
    "scope": {
      "type": "string",
      "description": "NPM scope of this package"
    },
    "publishable": {
      "type": "boolean",
      "description": "Whether or not this library is publishable",
      "default": true
    }
  },
  "required": ["name", "scope"]
}
