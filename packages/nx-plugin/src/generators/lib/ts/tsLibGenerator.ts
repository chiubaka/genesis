import { Tree } from "@nrwl/devkit";

import { LibGeneratorSchema } from "../libGenerator.schema";

export async function tsLibGenerator(
  _tree: Tree,
  _options: LibGeneratorSchema,
) {
  await Promise.resolve();
}
