{
  "$schema": "http://json-schema.org/schema",
  "$id": "TsConfigProject",
  "cli": "nx",
  "title": "Create a TypeScript configuration files for a project",
  "description": "Create a TypeScript configuration files for a project",
  "type": "object",
  "properties": {
    "projectName": {
      "type": "string",
      "description": "The name of the project to generate configuration for"
    },
    "projectType": {
      "type": "string",
      "description": "The type of project to generate configuration for",
      "enum": ["application", "library"]
    }
  },
  "required": ["projectName", "projectType"]
}
